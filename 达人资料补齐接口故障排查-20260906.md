# 达人资料补齐接口故障排查记录（2026-09-06）

> **2026-09-09 更正：下面的「平台侧整体异常」结论不成立，勿据此等平台。**
>
> 同店实测（Web Worker 绕过页面 hook）：不带签名请求同一接口返回
> `{"code":0,"message":"success"}` + 真实达人数据；带 `X-Bogus`/`X-Gnarly` 则返回
> `{"code":100000,"message":"Please remove the plugin and try again"}`。签名由 TikTok
> 自己的 `byted_acrawler`/`webmssdk` 生成，但在本机紫鸟环境里被判成插件流量。
> 卸载达秘、禁用花漾TK 后整页重载仍复现，官方详情页同样「数据加载失败」，
> 说明触发点是紫鸟浏览器环境本身（自带扩展 `Ziniao Browser` 主世界 hook
> `window.Notification`，页面里可读到 `BROWSER_HACKED_NOTIFICATION`），不是业务插件、
> 也不是平台故障。代码侧已加系统级故障熔断（连续 3 行停止逐行回退，见 AGENTS.md）。
> 处置方向：找紫鸟确认/升级指纹，或用干净浏览器 profile 复测。

> 结论先行（**已作废，见上**）：**TikTok 平台侧数据服务当前整体异常**（官方自己的达人详情页同样报错），
> 不是我们的代码或请求契约问题。修复方向是等平台恢复，而不是改代码。

---

## 1. 报错类型

页面同源只读 API 业务错误（HTTP 200，业务码非 0）：

```
/api/v1/oec/affiliate/creator/marketplace/profile 业务失败 code=100000 message=；未将空指标当作无数据
```

原始响应体为裸 JSON：`{"code":100000}` —— 无 message、无 data、无 request_id。

- 触发位置：`fill_creator_type_from_profile_api`
  （`assistant/services/creator_enrich_service.py`）。
- 请求构造：`scripts/lib/creator_api.py`，`PROFILE_TYPES = (2, 3, 4, 5)`，对每位达人发 4 次请求。
- 传输层：`scripts/lib/page_api.py`，`CREATOR_PROFILE_ENDPOINT` + `PageApiBusinessError`。

## 2. 影响范围与时间线

| 项 | 值 |
|---|---|
| 影响任务 | `creator_enrich` / `followup_generate`（先补齐达人资料再生成跟进） |
| 数量 | 57 位达人**全部**失败（`type_filled=0`、`type_failures=57`） |
| 首次观察到 | 2026-09-06 北京时间 10:10（`creator_enrich` 任务，即报错代码首次出现） |
| 持续情况 | 11:44 / 11:57 / 13:36 / 13:40 多次运行均全挂，14:00 前后现场复测仍全挂 |
| 最后正常 | 2026-08-27 10:36（`creator_enrich` 成功，`type_filled=11/15`） |

## 3. 现场证据链（排除法）

1. **接口路径没变**：当前官方 bundle（`main.7bf53cbb.js`）里 `GetCreatorProfile`
   仍指向同一 POST 路径。
2. **官方契约已改方向**：官方当前枚举为
   `PROFILE_BASIC:1, PROFILE_PERFORMANCE:2, PROFILE_PORTRAIT:3, PROFILE_TREND:4, PROFILE_VIDEO:5, PROFILE_PARTNER:6`；
   官方详情页现在**一次只传 `profile_types:[1]`**（PROFILE_BASIC），从单个
   `creator_profile` + `creator_profile_trend_data` + `creator_connect_info` 取数。
   我们还在按旧的拆分方式发 `[2,3,4,5]` 四次请求。
3. **但按官方最新契约重放也失败**：在同一登录会话分别重放 `profile_types:[1]`、`[2]`、`[6]`
   → 全部 HTTP 200 + `{"code":100000}`。即：**不是我们发错了**，官方当前契约同样拿不到数据。
4. **官方自己的页面也在报错**：达人详情页每个模块（合作指标 / 视频数据 / 直播数据 /
   粉丝数 / 趋势 / 热门视频 / 电商视频）全部显示「出错了 数据加载失败，请稍后刷新 重试」——
   官方前端同会话同样读不到达人数据。
5. **不是会话 / 网络 / 登录问题**：同一窗口同一会话，样品列表 API（11:30 导出成功）和
   物流 API（10:07 同步成功）都正常。
6. **错误形态特征**：`code=100000` 且无 message，是典型的「通用系统错误」；
   对比同类接口的真实参数校验错误（如 `sample/group/list` 传参错误）会返回带 message 的
   `98001004 Invalid parameters`。

## 4. 联网排查结论

检索渠道：本地 smart-search CLI（Exa / Tavily / Firecrawl 多引擎），中文 + 英文。

### 4.1 未找到同款事故的公开报道

中文卖家社区（AMZ123、卖家大学、知识库文章）和英文社区均无「2026-09-06 前后、
达人详情接口 code=100000 / 达人详情页全模块数据加载失败」的帖子。

### 4.2 检索到的相关（但非相同）内容

| 内容 | 与本次问题的关系 |
|---|---|
| 通用故障排查指南（多种来源，内容一致） | 判别表明确：**所有**达人都打不开 + 主账号也不行 + 换无痕/换网络/换设备依旧 → 大概率是 **TikTok Shop 后台接口故障**，反复刷新无效，等恢复或提工单。与本次现场现象完全吻合 |
| TikTok 官方文档（定向合作、联盟使用指南等） | 无任何关于该端点或 `100000` 码的公开说明 |
| 抖音开放平台通用错误码（28001005 系统繁忙 / 900933 权限类错误等） | 是**抖音**（国内站）开放平台，不同平台、不同码，仅作类比 |
| Shopify 社区：TikTok Shop 连接 Error 1105 throttled | 是 Shopify 集成被限流，不同场景 |
| TikTok for Developers 文档 | 无该端点的公开定义 |

### 4.3 结论

- **不是**我们代码 / 请求契约的问题（官方当前契约重放同样失败）。
- **不是**会话 / 网络 / 紫鸟通道问题（同会话其它接口正常）。
- 是 TikTok 平台侧该数据服务当前异常：对所有请求返回裸通用错误 `{"code":100000}`；
  账号级风控可能性低但无法 100% 排除（若为风控，官方 UI 同样会报错，表现一致）。

---

## 5. 建议动作

1. **不要现在改 `scripts/lib/creator_api.py` 的契约**。官方当前契约 `[1]` 现在同样失败，
   按「先实测再写死」纪律：等平台恢复后先实测 `profile_types:[1]` 的完整响应字段，
   再评估是否改为单次请求 + 新解析。
2. **等官方页面恢复后再跑补齐**。恢复判断标准：在紫鸟窗口打开任意达人详情，
   官方页面不再显示「数据加载失败」。
3. 上一步已删除「连续 3 次失败提前停止」熔断 —— 平台恢复后一次即可补齐全部 57 位，
   失败会逐位记账并继续，不会提前中断。
4. **可提工单**（可选）：Seller Center → Help/Contact us，附：
   - 店铺 ID：`7496019476093176674`（跨境2号店）
   - 首次出现时间：2026-09-06 北京时间 10:10 起持续至今
   - 现象：联盟中心 → 达人详情页所有模块「数据加载失败，请稍后刷新」
   - 关键证据：官方前端 bundle `main.7bf53cbb.js` / `creator_submodule_global 2.0.0.57`，
     其自身请求（`creator_oec_id + profile_types:[1]`）也返回裸 `{"code":100000}`
5. 同日 `shipment_sync`（10:07）2 单物流读取失败（订单 ****3700、****2768，`PageApiError`）
   属单订单瞬时失败，已跳过，下一轮自动补，无需处理。
