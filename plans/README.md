# Implementation Plans

Generated by the improve skill on 2026-08-21 against commit `a23807c`.
**Reviewed 2026-08-22** (`/improve review-plan @plans`). HEAD 仍是 `a23807c`；SOP 2、指定 B005、`未发布`、跟进语言优先级在 **未提交工作区**，不在该 commit 里。

Source spec (not executable): [`plan/plan_workdesk_v1.md`](../plan/plan_workdesk_v1.md).

Execute in the order below unless a dependency is BLOCKED. Each executor: read the assigned plan fully before starting, honor its STOP conditions, and update your row when done. Do not implement later milestones from the spec.

## Drift（所有计划共用）

`git diff a23807c..HEAD` 在当前仓库是空的，**不能**当 drift 检查。执行器第一步用：

```bash
git rev-parse --short HEAD   # 期望 a23807c；若已前进，再 diff 新 HEAD
git diff --stat a23807c -- <该计划 in-scope 与 Current state 列出的路径>
git status --short -- <同上>
```

`git diff --stat a23807c -- path` 含工作区未提交改动。2026-08-22 审查时 **已知且必须沿用** 的工作区事实（不要当意外 drift 回退）：

| 路径 | 必须沿用的内容 |
|------|----------------|
| `scripts/lib/filters.py` | `ACTIVE_HERO_PRODUCT_ID = "1732414717062320994"`；空 `allowed_product_ids` = 全部主推 |
| `scripts/lib/feishu_bitable.py` | `COOPERATION_STATUS_UNPUBLISHED = "未发布"`（D+15 未履约；**不是** `待发布`） |
| `样品申请筛查sop/2-查看到货+达人跟进.md` | 日历、指定 B005、话术原文（HEAD 无此文件；旧合并 SOP 已删） |
| `AGENTS.md` | 跟进只扫免费样品 `tab=40`；语言先飞书「使用语言」；不扫买返 |

若这些符号/文件消失或改义 → STOP，不要用 `plan/plan_workdesk_v1.md` 的旧日历（D+5 / 每隔两天 / B006-A）补洞。

测试基线（2026-08-22）：`python3 -m unittest discover -s tests -q` → `Ran 254 tests` `OK`。执行后以 exit 0 为准，不要为凑 254 去删无关测试。

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | SOP2 跟进领域模型（纯函数） | P1 | M | — | DONE |
| 002 | 物流详情解析（只读、缺字段失败关闭） | P1 | M | 001 | TODO |
| 003 | 本地操作台骨架（FastAPI + SQLite + 本机会话） | P1 | M | — | TODO |
| 004 | 持久任务执行器与 SSE | P1 | M | 003 | TODO |
| 005 | 只读物流同步 + 今日待办 + 话术预览 + CSV | P1 | L | 001, 002, 003, 004 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

**推荐串行：001 → 003 → 002 → 004 → 005。** 001 与 003 仅当遵守下列合并协议才可并行：

- 001 只创建 `assistant/domain/**` 与 `tests/assistant/test_{shipment_status,followup_stage,policies,sku_images,message_templates}.py`。
- 003 若看到 `assistant/domain/` 已存在：**keep**，不得清空、不得改 domain 文件。
- 003 drift 的 `assistant/` 排除 `assistant/domain/`。
- 两边都只改 `plans/README.md` **自己那一行** Status，禁止重写整张表。
- `__init__.py`：谁先做谁创建空文件；后到者若已存在则不动。

002 必须在 001 之后（要调用 `normalize_shipment_status`，禁止在 `order_api.py` 再写一套状态机）。004 必须在 003 之后。005 必须等前四份都 DONE。

## 原规格的结论

`plan/plan_workdesk_v1.md` 是一份可用的 **产品/架构规格**，不是给弱执行模型的 handoff plan。作为执行说明书，它缺 executor 指令、drift、摘录、逐步 verification、文件级 scope、机器可检查 Done criteria。

执行器若直接按原规格开工，会把 M1–M6 一次做完，并误把 SOP 7–9 的 `sync_shipped_tracking.py` 包成 SOP 2 的「到货跟进」。本目录把第一轮收敛到原规格自己建议的 **M3**（可启动 + 可查看物流 + 可安排待办 + 预览话术 + 导出），并且把规格里与 2026-08-22 已拍板事实冲突的假设改掉。

M4 内容证据、M5 私信/飞书 **写入**、M6 安装包：**不要做**。等 M3 在真实物流字段上稳定后再另写计划。

## Dependency notes

- 002 依赖 001 的 `normalize_shipment_status`。
- 005 的日历依赖 001 + 002。002 路径已按真实 payload 钉死。**D0 只在免费样品「处理中」`tab=40`**：2 号店 49 单物流最新 title 均为 `Delivered`，用该事件 unix 秒。已发货 `tab=30` 是在途。**D+10 名单只出 `curr_status=40` 的行，不扫已完成。** `delivered_at` JSON 键不存在。
- 004 的 worker 线程依赖 003 的 SQLite engine（WAL + `check_same_thread=False`）和本机会话。
- 005 调紫鸟时必须走 004 的单 worker + 店铺锁。**例外：** `GET /api/stores` 与诊断页的 `list_running_stores` / `probe_store_page` 允许在只读 GET 里调用；若已有 ziniao job `running`，这两个 GET 改读 DB 缓存，禁止并行 `zclaw_exec`。
- `lib.order_api.parse_logistics_details` 与 `scripts/launch_assistant.py` 必须把 **仓库根** 插入 `sys.path`（`order_api` 用 `Path(__file__).resolve().parents[2]`）。现有 CLI 只插 `scripts/`，否则 `import assistant` 会把 SOP 7–9 和 `discover -s tests` 一起打爆。懒加载 assistant，不要在 `order_api` 模块顶层 import。
- 处理中列表：`scrape_pending_list_api(..., tab=40, ensure_page=False)`。默认 `ensure_page=True` 会点「待审核」tab，禁止。
- 指定跟进款：`lib.filters.ACTIVE_HERO_PRODUCT_ID`。001 配图与 005 待办过滤都 import 它，不要再抄一遍数字。
- M3 **可以只读** 飞书「使用语言」；**不可以写** 合作状态 / 新建达人行 / 快递单号。

## Findings considered and rejected

- **把 FastAPI 接到现有 CLI 子进程并解析 stdout**：规格允许「短暂过渡」。拒绝作为目标形态，也拒绝作为 M3 实现。现有脚本有确认门闩、16:00 门、`--execute`；子进程适配器很容易误触发写操作或继承错 SOP。
- **跟进幂等键包含 `template_version`**（原规格 §7.3）：话术改版会为同一天同一阶段再插一条待办。唯一键改为 `(sample_case_id, stage, scheduled_for)`；版本只记录在行上。
- **M3 读写飞书「合作状态」**（含首页展示飞书状态、D+15 写「未发布」、内容确认后写「已完成」）：第一轮不写飞书。页面合作状态显示「未同步」。**例外（审查改口）：** generate 允许只读「使用语言」；读失败则 `en` + needs_review，不新建行。
- **诊断页用 `resolve_store_id` 的测试默认 1 号店**：CLI 在 running 不唯一时可落到 `27437742526069`。操作台在 0 家或多家 running 时必须报错，不得默认切到 1/2 号店。
- **`shipment_sync` 继承北京时间 16:00 门**：该门只约束 SOP 7–9 写飞书/发物流私信（`sync_shipped_tracking.py`）。SOP 2 只读物流同步必须全天可跑。
- **在 M3 实现 `message_send` / `feishu_update` / 批准**：原规格把接口都画了。不可撤销写操作留到单独计划；现有 CLI 入口保持应急。
- **用 GPM 自动拆两条消息或自动选型**：筛查导出「视频达人/直播达人」=「是」或空，可同时为是；**≠** 该侧已过 SOP。跟进只读这两列原文；两列都是「是」或都空 → 人工。禁止 `derive_creator_type_flags` 的 GPM 兜底。
- **把 `template_version` 写进幂等键、或按标题/描述模糊匹配 B005**：与主推精确匹配纪律冲突。只认商品 ID `1732414717062320994`。
- **B006-A 配图 / 跟进其它主推款**：当前跟进池只有指定 B005。`arrival_other_*` 话术保留给以后 `--all-hero-products` 打开跟进时用；M3 generate 不为其它 `product_id` 建任务。
- **感谢话术加「投流码」或其它原文没有的句**：严格复制 SOP 2「达人出视频 / 达人开启直播」段落。
- **D+10 扫已完成（tab=53 等）**：抽样本里有旧 Delivered，但已拍板 **只出处理中名单**。已完成走感谢/已完成，不进 D+10。
- **扫买返样品**：买返是另一套页签；点了买返后 `group/list` 不变。V1 只扫免费样品。
- **用 ETA / `predict_delivery_time_text` 当 D0**：ETA 是预计，不是送达。
- **D+5 / 「每隔两天」循环**（原规格与旧 wiki）：以本地 SOP 2 项目约定为准。
- **PyInstaller / 签名公证（原 Phase 8）**：原规格已把 M6 放到 M3 之后。M3 入口是仓库内 `scripts/launch_assistant.py`。
- **根目录 `migrations/` 与 `assistant/database/migrations/` 并存**：只保留 `assistant/database/migrations/`。
- **用 `ziniao-cli page extract --mode running` 做店铺诊断**：可能 `runtime.reopen`。必须调用已有 `scripts/lib/zclaw.py` 的 `list_running_stores()`（内部是 `zclaw invoke extract_data`）。

## 2026-08-22 review-plan 改了什么

执行器只读各 `00N-*.md` 即可；本段给后人看审查动机。

- Drift 从 `SHA..HEAD` 改成含工作区的 `git diff --stat a23807c -- paths`。
- 001 配图常量改为 import `ACTIVE_HERO_PRODUCT_ID`；删掉未再使用的 `is_overdue_unpublished` 与「D+11 overdue」测试。
- 005 删除「扫已完成 Delivered 子集」；首页计数去掉 D+5/循环；配图改走 `product_id`；允许只读「使用语言」。
- 003 `sample_cases` 补 `curr_status` / `is_video_creator` / `is_live_creator` / `feishu_lang`，避免 005 再迁列。
- 处理中列表必须 `ensure_page=False`。
- 冷读补丁：仓库根 `sys.path`、配图 static 映射、只走 `navigate_to_sample_request`、B005 前缀匹配寄样产品、`confirm_delivery_time` 哨兵日期、tab=40 禁止 DOM 回退到已发货、001∥003 合并协议。
