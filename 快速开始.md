# 快速开始

新电脑配到能双击跑样品筛查。配完后日常只认 [README.md](README.md)。

只走 **紫鸟 GUI + 本机 ZClaw**。不要申请 WebDriver，不要配 `ZINIAO_*`。

---

## 1. 本机要有的东西

| 项 | 要求 |
|----|------|
| 系统 | macOS，或 Windows（amd64 / arm64） |
| 紫鸟 | GUI 已装，能登录并看见工作台。安装包找技术人员 |
| Node / npm | 官网 LTS（Node ≥ 18，自带 npm） |
| Python | 官网 3.x。须 3.9+；读 `config.toml` 建议 **3.11+**（自带 tomllib）。Windows 验收用 `py -3` |
| 本仓库 | 任意目录；命令一律在**仓库根目录** |
| 飞书 | 根目录有 `config.toml`（从 `config.toml.example` 复制，Secret 找管理员） |

不要手改系统 `PATH`，启动器也不会改 PATH。不要用微软商店 Python、nvm / n / fnm、conda、Git Bash。

本机已有且第 7 节验收通过：跳过第 2 节。

---

## 2. 装 Node 和 Python（只走官网安装包）

装完必须**关掉所有终端窗口**，新开一个再验收。不要 `sudo npm`。

### Node.js（带 npm）

打开 [https://nodejs.org/en/download](https://nodejs.org/en/download)，选 **LTS**，下载官方安装包：

- macOS：`.pkg`
- Windows：`.msi`（会写入用户 PATH，不必手加）

一路下一步。新开终端：

```bash
node -v    # 须 ≥ 18
npm -v
```

Windows 再确认（**cmd**，不要 Git Bash）：

```bat
where node
where npm
```

`npm install -g` 报 `EACCES`：用官网安装包重装 Node，不要 sudo。

### Python

打开 [https://www.python.org/downloads/](https://www.python.org/downloads/)，下当前 3.x 稳定版。

- macOS：官网 `.pkg`。装完到 `/Applications/Python 3.x/` 里跑一次 **Install Certificates.command**。不要只用系统自带的 `/usr/bin/python3`。
- Windows：官网的 **Python Install Manager**，或同页的 Windows 安装包（`.exe`）。
  - 传统安装包第一屏底部勾选 **Add python.exe to PATH**。这就是「加入 PATH」，不要再去「环境变量」里手改。
  - 即使忘了勾选，官网安装器通常仍会把 `py` 启动器放到 `C:\Windows\py.exe`。本仓库 Windows 入口优先用 `py -3`。
  - 不要用微软商店里点一下才跳转安装的占位 `python`。

新开终端验收：

```bash
# macOS
python3 --version    # 须 3.9+，建议 3.11+

# Windows（cmd 或 PowerShell）
py -3 --version      # 须 3.9+，建议 3.11+
where py
```

`python` 弹出 Microsoft Store：这是占位符，不是真 Python。按上面重装官网包，验收只认 `py -3`。

### 不要手改 PATH

官方安装包会写用户/系统 PATH。手改容易改错、和商店版/nvm 抢命令。

装完后：

1. 关掉所有 cmd / PowerShell / 双击弹出的黑窗口
2. 新开一个窗口再跑上面的 `where` / `--version`
3. **双击 `.bat` 仍找不到**：注销或重启一次。资源管理器不会热更新 PATH；双击用的是登录时的环境，不是你刚开的那个终端

`nvm-windows` 装的 `node` 常常只在某个终端里有，双击 `.bat` 看不到。卸掉，改用官网 MSI。

---

## 3. 装 ziniao-cli

在**新开的** cmd / PowerShell / macOS 终端：

```bash
npm install -g @ziniao-open/cli
ziniao-cli --version
```

Windows 再确认：

```bat
where ziniao-cli
dir "%APPDATA%\npm\ziniao-cli.cmd"
```

当前窗口找不到命令：关掉重开再试。不要为此改系统 PATH，启动器也不会 `set PATH`。  
Windows 用 PowerShell 或 CMD，不要用 Git Bash。

脚本不依赖「PATH 里刚好有 ziniao-cli」。它用 `which` 找到 `ziniao-cli` / `ziniao-cli.cmd` / `node`，再换成 `node` + `run.js` 的绝对 POSIX 路径。终端验收仍应能直接跑 `ziniao-cli --version`（证明官方安装包已写入用户 PATH）。

---

## 4. 写入紫鸟开放平台 apiKey

这是 [紫鸟开放平台](https://open.ziniao.com) 里**某个已审核应用**的 apiKey，给 `ziniao-cli` 鉴权本机 ZClaw。不是紫鸟登录密码，不是 TikTok Shop 店后台账密，也不是 WebDriver 的 `ZINIAO_*`。

向技术人员要企业里**已有**那一把。禁止发到聊天、禁止写入仓库。

```bash
ziniao-cli config init
```

选「手动输入 Key」。不要选新建应用，除非对方明确让你建。

`ziniao-cli config show` 看是否已保存。**不要**把完整 Key 打到终端。

---

## 5. 飞书 `config.toml`

```bash
# macOS
cp config.toml.example config.toml

# Windows（cmd）
copy config.toml.example config.toml
```

找管理员填两处 **App Secret**（主推表只读、达人关系表可写）。不要把填好的 `config.toml` 提交 git、不要发聊天。

---

## 6. 开紫鸟 GUI，登录

- macOS：从「应用程序」打开紫鸟
- Windows：开始菜单打开紫鸟

两端都不要走 WebDriver。用本人账号登录，工作台能看见店。然后**只打开一家店**，在店铺窗口登录 TikTok Shop（商家中心任意页即可，不必停在首页）。

---

## 7. 连通验收

紫鸟 GUI 保持打开。

### macOS

```bash
cd ~/Projects/zn_sample
python3 --version
node -v
npm -v
zsh scripts/ziniao-status.sh    # 须打印 mode: GUI
ziniao-cli --version
ziniao-cli doctor
ziniao-cli store list --format table
```

### Windows（cmd，不要 Git Bash）

先 `cd /d` 到本仓库根目录。

```bat
py -3 --version
where py
node -v
npm -v
where node
where ziniao-cli
ziniao-cli --version
ziniao-cli doctor
ziniao-cli store list --format table
```

不要跑 `scripts\ziniao-status.sh`。Windows 预检不看进程模式，只看工作台是否只开一家店。

`store list` 须能看见将要操作的那家店。`doctor` 连不上 Bridge：先保证紫鸟 GUI 已打开。

`config init` 成功只表示本地有配置。`doctor` / `store list` 仍报 API Key 认证失败时，把**这台电脑**的终端识别码绑到签发该 Key 的应用上：

1. **获取**：紫鸟 GUI 已登录 → 顶栏 **软件设置** → 左侧 **本地设置** 下的 **终端信息** → **终端识别码** → **复制**。
2. **配置**：浏览器打开 [open.ziniao.com](https://open.ziniao.com) → 进入**这把 apiKey 对应的那个应用** → **终端管理** → 绑上刚复制的识别码。自己进不去该应用时，把识别码发给技术人员代绑。

换电脑或重装客户端后识别码会变，要再绑一次。

---

## 8. 只开一家店，再双击

1. 工作台**只打开一家店**，其它店关掉。
2. 店铺窗口登录 TikTok Shop（商家中心任意页即可，不必停在首页）。
3. 按 [README.md](README.md) 双击「1-只出名单」。

不要两人同时操作同一家店。不要默认去开 2 号店。

---

## 配错通道

| | 本仓库 | 不要走 |
|--|--------|--------|
| 客户端 | **GUI**（有窗口） | WebDriver（通常无窗口） |
| 口 | `127.0.0.1:9481`（ZClaw） | `127.0.0.1:16851` |
| 凭证 | `config init` 的 apiKey | `ZINIAO_*` 企业账密 |

---

## 参考

| 来源 | 说明 |
|------|------|
| [Node.js 下载](https://nodejs.org/en/download) | 官方 LTS 安装包（含 npm） |
| [Python 下载](https://www.python.org/downloads/) | 官方 3.x 安装包 |
| [@ziniao-open/cli](https://www.npmjs.com/package/@ziniao-open/cli) | 官方 CLI |
| [紫鸟开放平台](https://open.ziniao.com) | 应用审核、终端管理 |
| `README.md` | 日常双击 |
| `AGENTS.md` | 通道、storeId、写操作门闩 |
