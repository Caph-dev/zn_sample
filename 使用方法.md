# 样品申请筛查使用方法（业务员版）

这份文档给不需要了解 Python、API 或页面结构的业务员使用。按照“复制命令 → 粘贴到终端 → 按 Enter”的方式操作即可。

## 一、先记住三条规则

1. **默认只读**：普通筛查只读取待审核列表和达人详情，并导出名单，不会点击「同意」「拒绝」，也不会写飞书或发送私信。
2. **正式筛查必须看详情和主推款**：命令必须带 `--with-detail --require-detail`；申请的样品还必须是飞书主推表中「是否主推=是」的商品。
3. **批准、写飞书、发私信都是单独的危险操作**：只有命令中明确出现 `--execute --yes` 或 `--write-feishu` 时才会执行对应写操作。第一次运行建议先做只读预演。

项目根目录：

```bash
cd ~/Projects/zn_sample
```

如果你的项目不在这个位置，请把命令中的路径换成实际项目路径。

---

## 二、正式筛查前准备

### 1. 第一次使用：配置飞书密钥

只需要配置一次。复制下面命令并按 Enter：

```bash
cd ~/Projects/zn_sample
cp -n config.toml.example config.toml
```

然后用编辑器打开 `config.toml`，在 `[feishu]` 中填写 `app_secret`。如果需要让脚本批准后自动写入达人关系表，还需要填写 `[feishu.bitable]` 的配置。

macOS 可以复制下面命令，用系统文本编辑器打开配置文件：

```bash
open -e config.toml
```

注意：

- 密钥不要发到聊天、截图或提交到 Git；
- 如果管理员已经配置过 `config.toml`，不要重复覆盖；
- 主推款读取的是飞书知识库中的 `tk产品图+货号`，不是本地 Excel 文件。

### 2. 检查紫鸟是否正常

```bash
ziniao-status
ziniao-cli doctor
```

常见含义：

- `ziniao-status`：查看紫鸟 GUI 是否运行；
- `ziniao-cli doctor`：检查紫鸟配置、登录状态和 Bridge 是否连通；
- 如果检查失败，不要继续运行筛查命令，先处理紫鸟问题。

本项目使用紫鸟 **GUI + ZClaw**。不要为了排查而运行可能触发店铺重开的旧命令：

```text
ziniao-cli page extract --mode running
```

### 3. 确认店铺编号

测试 1 号店：

```text
27437742526069
```

正式操作其他店铺时，使用管理员提供的 `store-id`，不要凭店铺名称猜编号。

---

## 三、脚本 1：打开店铺并完成登录

复制下面命令，把 `<店铺编号>` 换成实际编号：

```bash
python3 scripts/open_sample_store.py --store-id <店铺编号>
```

例如测试 1 号店：

```bash
python3 scripts/open_sample_store.py --store-id 27437742526069
```

脚本 1 只负责：

1. 确认目标店铺是否已经在紫鸟 GUI 中运行；
2. 未运行时打开目标店铺；
3. 已运行时不关闭、不重开；
4. 检查页面是否可以操作。

脚本成功后，业务员需要手动完成以下操作：

1. 在紫鸟窗口中登录 TikTok Shop；
2. 处理验证码、二次验证或登录提示；
3. 登录成功后停在 **TikTok Shop 商家中心首页**；
4. 不要停在登录页、空白页或达人详情页；
5. 确认店铺是本次要处理的店铺。

### 如果登录遇到问题

- 登录、验证码、短信验证由业务员手动完成，脚本不会代填账号密码；
- 如果页面仍显示登录页，先完成登录，不要运行脚本 2；
- 如果脚本提示有其他店铺正在运行，不要反复运行脚本 1，也不要让脚本自动切店，请先人工确认当前紫鸟窗口和目标店铺；
- 如果误停在达人详情页，可回到商家中心首页后再运行脚本 2；
- 如果出现 `runtime.reopen`、店铺被意外重开或窗口状态异常，先停止操作并联系技术人员，不要继续点击批准。

---

## 四、脚本 2：自动进入待审核并做正式只读筛查

确认已经登录并停在商家中心首页后，复制下面命令：

```bash
python3 scripts/screen_sample_requests.py \
  --store-id <店铺编号> \
  --from-seller-home \
  --data-source auto \
  --with-detail \
  --require-detail
```

例如测试 1 号店：

```bash
python3 scripts/screen_sample_requests.py \
  --store-id 27437742526069 \
  --from-seller-home \
  --data-source auto \
  --with-detail \
  --require-detail
```

粘贴完整命令后按 **Enter**。脚本会：

1. 从商家中心首页自动进入 **联盟中心 → 样品申请管理 → 待审核**；
2. 先读取列表数据做初筛；
3. 只对初筛有机会通过的人读取达人详情做复筛；
4. 强制检查申请商品是否为主推款；
5. 导出筛查结果。

### 为什么命令中要有这几个参数？

| 参数 | 作用 |
|------|------|
| `--store-id` | 指定本次要操作的店铺，避免操作错店；使用自动导航时必须填写 |
| `--from-seller-home` | 告诉脚本当前停在商家中心首页，由脚本自动进入待审核 |
| `--data-source auto` | 优先使用页面内只读 API；接口异常时自动回退现有页面读取方式 |
| `--with-detail` | 对初筛通过者读取达人详情 |
| `--require-detail` | 没有完整详情指标时不判定为通过 |

### 两轮筛选是怎么做的？

```text
待审核列表初筛
  ↓
只保留列表指标和主推款有机会通过的人
  ↓
读取达人详情复筛
  ↓
导出最终通过名单
```

列表初筛会看粉丝、GMV、成交件数、类目、履约率、女性粉丝比例等；详情复筛会看视频/直播 GPM、播放量、互动率、客单价等。视频或直播一侧达标即可满足对应条件。

**注意：** “视频达人”和“直播达人”两列是分类标记，不等于该侧一定通过全部 SOP；请以导出文件中的「是否通过」和「原因」为准。

### 如果已经手动停在待审核

可以不使用自动导航参数：

```bash
python3 scripts/screen_sample_requests.py \
  --store-id <店铺编号> \
  --data-source auto \
  --with-detail \
  --require-detail
```

脚本会直接检查当前页面并开始筛查。

---

## 五、查看筛查结果

运行结束后，脚本会显示导出文件位置。默认文件在：

```text
exports/
```

通常会生成：

- `.xlsx`：方便业务员查看和筛选；
- `.csv`：方便其他表格工具导入；
- `.json`：供后续介绍私信脚本使用；
- `*_shadow.json`：只有使用 `shadow` 对比模式时才会生成。

建议重点查看：

- 达人名 / 昵称；
- 产品货号和商品 ID；
- 是否主推；
- 是否通过；
- 不通过原因；
- 视频 GPM、直播 GPM；
- 视频达人、直播达人；
- 详情读取是否成功。

### 常用的安全选项

只导出通过名单：

```bash
python3 scripts/screen_sample_requests.py \
  --store-id <店铺编号> \
  --from-seller-home \
  --data-source auto \
  --with-detail \
  --require-detail \
  --eligible-only
```

测试时只扫 6 行：

```bash
python3 scripts/screen_sample_requests.py \
  --store-id <店铺编号> \
  --from-seller-home \
  --data-source auto \
  --with-detail \
  --require-detail \
  --max-rows 6
```

指定输出文件前缀：

```bash
python3 scripts/screen_sample_requests.py \
  --store-id <店铺编号> \
  --from-seller-home \
  --data-source auto \
  --with-detail \
  --require-detail \
  --out exports/my_sample_screen
```

`--max-rows 6` 只适合测试；正式筛查不要限制行数。不要使用已经移除的 `--detail-limit`，也不要正式使用 `--skip-hero-check`。

---

## 六、批准样品申请（危险操作，运行前必须确认）

普通筛查完成后**不会自动批准**。只有在命令中同时出现 `--execute --yes` 才会点击「同意」，而且永远不会点击「拒绝」。

### 只批准，不写飞书

确认导出结果和候选人无误后，复制：

```bash
python3 scripts/screen_sample_requests.py \
  --store-id <店铺编号> \
  --from-seller-home \
  --data-source auto \
  --with-detail \
  --require-detail \
  --execute \
  --yes \
  --execute-limit 1
```

这里推荐 `--data-source auto`：列表和详情优先走只读 API，接口异常时回退页面读取。批准写来源默认是 API。API 写入失败或状态不确定时不会自动重试或回退 DOM。

脚本会先备份本次结果，再重新检查申请是否仍在待审核、达人和商品是否一致，批准后还会再次确认申请进入「待发货」。默认一次最多批准 1 条。平台批准成功后不能由脚本撤销。

如需使用旧的页面点击路径，显式增加：

```text
--write-source dom
```

### 批准后同时写入飞书达人关系表

确认 `[feishu.bitable]` 已配置后，在上一个命令最后增加：

```bash
  --write-feishu
```

完整示例：

```bash
python3 scripts/screen_sample_requests.py \
  --store-id <店铺编号> \
  --from-seller-home \
  --data-source auto \
  --with-detail \
  --require-detail \
  --execute \
  --yes \
  --execute-limit 1 \
  --write-feishu
```

写入顺序是：

```text
平台批准成功
  → 确认申请已移出待审核
  → 写入飞书「内部达人建联记录表」-「达人关系管理（新）」
```

目标表链接：

<https://rsed6zggjt.feishu.cn/base/CJXSbLIQWahB8esVOiscX7j1nLc?from=from_copylink>

新记录默认填写：

- 人员：`王良希（技术）`；
- 合作状态：`待发货`；
- 是否已寄样：不勾选；
- 寄样产品：按主推表货号映射。

如果批准结果无法确认，脚本会停止并标记 `unknown`，不会写飞书，也不会自动重试。

---

## 七、批准后的第 6 步：发送介绍私信

介绍私信是独立脚本，不会在批准命令结束后自动发送。

### 先只读预演

把 `<筛查结果.json>` 换成上一次筛查生成的 JSON 文件：

```bash
python3 scripts/send_sample_intro.py \
  --from-export <筛查结果.json> \
  --max-rows 1
```

预演会打开达人详情，读取达人简介，判断英语或西班牙语，并显示将要发送的话术，但不会发送。真实发送默认走页面内 IM SDK，不点「发送」按钮。

### 确认后真实发送 1 条

```bash
python3 scripts/send_sample_intro.py \
  --from-export <筛查结果.json> \
  --execute \
  --yes \
  --execute-limit 1
```

语言规则：

- 只根据达人详情简介判断英语 / 西班牙语；
- 空简介默认英语；
- 如果平台不允许给该达人发消息，脚本不会绕过平台限制；
- 已有相同话术时会跳过，避免重复发送；
- 永远不要点击「邀请」。

从筛查导出导入时，会优先选择 `approve_status=approved` 的达人。

---

## 八、第 7–9 步：已发货、物流回写和物流私信

这三个步骤由另一个脚本处理，并且**不是批准后自动触发**。业务员需要在后续时间点重新运行命令。

### 第 7 步：读取已发货和物流单号

正式 SOP 要求北京时间 16:00 后处理。脚本每次启动都会用北京时间判断：不到 16:00 默认停止，不会到点自动运行。16:00 后直接跑即可；仅测试时可以加 `--force` 绕过时间门槛。可用 `--creator-name <达人名>` 只处理一个人。

只读预演（16:00 后）：

```bash
python3 scripts/sync_shipped_tracking.py \
  --store-id <店铺编号> \
  --max-rows 1
```

16:00 前测试：

```bash
python3 scripts/sync_shipped_tracking.py \
  --store-id <店铺编号> \
  --force \
  --max-rows 1
```

`--force` 只绕过时间检查，不代表会自动写飞书或发送消息。

脚本会读取：

- 已发货列表里的 `main_order_id`，对应飞书「订单号」；
- 商家订单物流接口 / 页面的 **TikTok 物流** 单号，对应飞书「快递单号」。

默认使用 API 读取；失败时可让技术人员改用 `--data-source auto` 或 `--tracking-source auto`。

第 9 步发送的是 TikTok 物流单号，不是订单 ID。

### 第 8 步：写入飞书

确认只读预演结果正确后运行：

```bash
python3 scripts/sync_shipped_tracking.py \
  --store-id <店铺编号> \
  --write-feishu \
  --max-rows 1
```

成功写入后：

- 写入订单号和 TikTok 物流单号；
- 勾选「是否已寄样」；
- 只在允许时把「合作状态」从 `待发货` 改为 `待发布`；
- 不会把「已发布」等后续状态改回去。

如果飞书已有不同物流单号，默认不覆盖，也不推进合作状态；需要管理员明确确认后才能使用 `--overwrite`。

### 第 9 步：写回后发送物流私信

只有当第 8 步写入成功或飞书已有相同物流单号时，才允许发送。真实发送 1 条：

```bash
python3 scripts/sync_shipped_tracking.py \
  --store-id <店铺编号> \
  --write-feishu \
  --send-tracking \
  --execute \
  --yes \
  --execute-limit 1
```

脚本发送英语或西班牙语固定物流话术，并接上 TikTok 物流单号。物流私信默认也走 IM SDK。第 7 步不会自动触发第 8、9 步；是否写飞书、是否发送，分别由 `--write-feishu` 和 `--send-tracking --execute --yes` 控制。

---

## 九、常见问题

| 看到的提示 / 情况 | 应该怎么做 |
|---|---|
| 页面还在登录页 | 手动完成登录、验证码或二次验证，再回到商家中心首页 |
| `not-on-sample-page` | 手动回到「样品申请管理 → 待审核」，或从首页重新运行脚本 2 |
| `no-pending-tab` | 确认当前店铺和页面正确；不要点击拒绝或批准，联系技术人员 |
| 扫到 0 行 | 等待页面加载，确认是在「待审核」而不是其他 tab；必要时重新运行只读命令 |
| 主推款全部不匹配 | 检查飞书 `tk产品图+货号` 的「是否主推=是」、货号和商品 ID |
| 飞书主推表读取失败 | 检查 `config.toml` 中 `[feishu] app_secret` 和网络权限 |
| `Bridge` 连接失败 | 运行 `ziniao-cli doctor`，确认紫鸟 GUI 正常；不要反复点击页面 |
| 出现 `runtime.reopen` | 停止操作，确认店铺是否被重新打开并联系技术人员；不要继续批准 |
| 没有 TikTok 物流单号 | 订单页可能尚未出单；不要把订单 ID 当物流单号发送 |
| 飞书没有匹配记录 | 脚本不会自动新建物流记录，先人工核对达人名和寄样产品 |
| 私信不可用 | 可能是平台限制或会话未建立；脚本不会点击「邀请」绕过限制 |

如果命令运行时间较长，请等待终端出现“完成”或导出文件路径，不要中途关闭紫鸟窗口或重复启动同一命令。

---

## 十、命令速查表

| 命令 / 参数 | 业务含义 |
|---|---|
| `ziniao-status` | 查看紫鸟 GUI 状态 |
| `ziniao-cli doctor` | 检查紫鸟和 Bridge |
| `open_sample_store.py` | 脚本 1：打开指定店铺，不自动关闭或切换其他店 |
| `--from-seller-home` | 从商家中心首页自动进入待审核 |
| `--data-source auto` | 只读 API 优先，失败时页面读取兜底 |
| `--with-detail` | 初筛后读取达人详情 |
| `--require-detail` | 缺详情数据不通过 |
| `--eligible-only` | 只导出通过名单 |
| `--max-rows 6` | 测试时最多扫 6 行 |
| `--detail-all` | 所有行都读详情，较慢，主要用于排查 |
| `--out exports/name` | 指定导出文件名前缀 |
| `--execute --yes` | 允许真实批准或发送私信，危险 |
| `--execute-limit 1` | 最多真实处理 1 条，建议保留 |
| `--write-source api` | 默认批准方式：调用已捕获的单条批准 API |
| `--write-source dom` | 备用批准方式：页面上点击「同意」；需要技术人员明确指定 |
| `--write-feishu` | 允许写入飞书，需单独确认 |
| `--force` | 测试时绕过北京时间 16:00 门槛 |
| `--send-tracking` | 在物流回写条件满足后发送物流私信 |

不使用：`--detail-limit`（已移除）、`--skip-hero-check`（正式流程禁止）。

---

## 相关文档

- [README.md](./README.md)：项目能力和边界概览；
- [AGENTS.md](./AGENTS.md)：技术约定、安全门闩和维护规则；
- [样品申请筛查 SOP](./样品申请筛查sop/样品申请筛查sop.md)：业务原始规则；
- [0814 SOP 与自动化对照](./SOP_0814_FLOW_COMPARISON.md)：SOP 步骤和当前实现状态。
