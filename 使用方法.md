# 样品申请筛查 · 操作手册

给业务员：复制命令 → 粘贴到终端 → 按 Enter。  
不用懂 Python。每做完一步再做下一步，脚本**不会**自动进入下一步。

开始前先进入项目目录：

```bash
cd ~/Projects/zn_sample
```

把命令里的 `<店铺编号>` 换成管理员给你的编号。测试 1 号店用 `27437742526069`。

**三条死规定**

1. 普通筛查只出名单，不会点「同意」，也不会写飞书、发私信。
2. 要批准或真发私信，命令里必须同时有 `--execute --yes`。第一次先跑**不带**这两项的预演。
3. 一次只处理 1 个人（保留 `--execute-limit 1`）。不要点「拒绝」或「邀请」。

---

## 0. 每天开始前

**飞书密钥只配一次。** 管理员已经配好 `config.toml` 就跳过。不要把密钥发到聊天或截图。

```bash
ziniao-status
ziniao-cli doctor
```

这两条都正常，再往下。失败就先找人看紫鸟，不要继续。

```bash
python3 scripts/open_sample_store.py --store-id <店铺编号>
```

然后你在紫鸟窗口里自己登录（验证码也是你点）。停在 **TikTok Shop 商家中心首页**。  
不要停在登录页、空白页或达人详情页。店已开着时脚本不会重开。

---

## 1. 只读筛查（出名单）

确认已停在商家中心首页：

```bash
python3 scripts/screen_sample_requests.py \
  --store-id <店铺编号> \
  --from-seller-home \
  --data-source auto \
  --with-detail \
  --require-detail
```

等终端出现「完成」和 `exports/` 路径。不要中途关紫鸟、不要连按两次。

打开 `exports/` 里最新的 `.xlsx`，看「是否通过」和「原因」。通过的人才能进入下一步。

测试可以在命令最后加 `--max-rows 6`（只扫 6 行）。正式筛查不要加。

---

## 2. 批准 1 条（可选，危险）

用上一步的 JSON，**不要再扫全表**。把文件名换成你刚才导出的那个。

```bash
python3 scripts/screen_sample_requests.py \
  --store-id <店铺编号> \
  --from-export exports/sample_screen_<时间戳>.json \
  --execute \
  --yes \
  --execute-limit 1
```

要同时写入飞书达人关系表，在最后再加一行 `--write-feishu`（须管理员已配好 `[feishu.bitable]`）。

成功后：接口接受就算批准成功。平台大约 **10 分钟** 才会从「待审核」转到「待发货」。  
**不要马上再跑一遍批准。** 平台同意不能撤销。

---

## 3. 补写飞书（约 10 分钟后）

人已经能在「待发货」里看到，但飞书还没写上时：

```bash
python3 scripts/screen_sample_requests.py \
  --store-id <店铺编号> \
  --from-export exports/sample_screen_<时间戳>.json \
  --confirm-export \
  --write-feishu
```

这条**不会重新批准**，只核对名单并补写飞书。

---

## 4. 介绍私信（第 6 步，危险）

批准命令**不会**自动发私信。一次一个人。

达人编号 `creator_id` 在导出 JSON 里。先预演：

```bash
python3 scripts/send_sample_intro.py \
  --store-id <店铺编号> \
  --creator-id <达人编号> \
  --creator-name <达人名>
```

看终端里的语言和话术。空简介会按**英语**发，即使聊天记录是西语也不会自动改。话术不对就停，找人改。

确认无误后再真发：

```bash
python3 scripts/send_sample_intro.py \
  --store-id <店铺编号> \
  --creator-id <达人编号> \
  --creator-name <达人名> \
  --execute \
  --yes \
  --execute-limit 1
```

不要点「邀请」，不要先点页面底部「聊天数」。脚本打不开会话就停，不要自己乱点。

---

## 5. 物流回写飞书（第 7–8 步）

**北京时间下午 4 点之后**再跑。不到 4 点脚本会直接拒绝。4 点后不用加 `--force`。

达人必须已经出现在「已发货」里。先预演（不写飞书）：

```bash
python3 scripts/sync_shipped_tracking.py \
  --store-id <店铺编号> \
  --creator-name <达人名>
```

看导出里的 **TikTok 物流单号**。没有单号就再等，不要写表，也不要把订单号当成快递单号。

预演没问题后再写飞书：

```bash
python3 scripts/sync_shipped_tracking.py \
  --store-id <店铺编号> \
  --creator-name <达人名> \
  --write-feishu
```

成功后会填订单号、快递单号，勾选「是否已寄样」，并把合作状态从「待发货」改成「待发布」。飞书对不上这个人+这个产品时**不会新建行**。

---

## 6. 物流私信（第 9 步，危险）

只有第 5 步已经写出快递单号（或飞书里已有同一单号）才发。发给达人的是 **TikTok 物流单号**，不是订单号。

```bash
python3 scripts/sync_shipped_tracking.py \
  --store-id <店铺编号> \
  --creator-name <达人名> \
  --write-feishu \
  --send-tracking \
  --execute \
  --yes \
  --execute-limit 1
```

---

## 出问题对照

| 看到什么 | 你怎么做 |
|---|---|
| 还在登录页 | 自己登录完，回到商家中心首页，再跑第 1 步 |
| `not-on-sample-page` / `no-pending-tab` | 手动回到「样品申请 → 待审核」，或从首页重跑第 1 步 |
| 扫到 0 行 | 确认是「待审核」tab，等列表加载完再跑 |
| `Bridge` 失败 | 再跑 `ziniao-cli doctor`；不要反复点页面 |
| 出现 `runtime.reopen` | **停手**，找技术人员，不要继续批准 |
| 飞书主推表读失败 | 找管理员查 `config.toml` |
| 批准后飞书没写上 | 等约 10 分钟跑第 3 步补写，不要再 `--execute` |
| 找不到私信会话 | 不要点「邀请」；仍失败就停 |
| 现在不到下午 4 点 | 物流命令会拒绝，等到 4 点再跑第 5 步 |
| 没有 TikTok 物流单号 | 还没出单，再等；不要发订单号 |
| 飞书没有匹配行 | 核对达人名和寄样产品；脚本不会新建物流行 |

---

更细的能力边界和 6 条命令总表：[README.md](./README.md)  
业务规则原文：[样品申请筛查 SOP](./样品申请筛查sop/样品申请筛查sop.md)
