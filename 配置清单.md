# zn_sample · 配置清单（交接用）

把本项目交给同事使用时，需要配置/申请的全部事项：**谁提供、写到哪里、怎么验收**。

| 文档 | 说明 |
|------|------|
| 本文 | 交接清单：哪些要配、找谁、怎么配 |
| [快速开始.md](./快速开始.md) | 新电脑逐步装机（安装包、命令细节） |
| [README.md](./README.md) | 日常双击、出问题 |
| [AGENTS.md](./AGENTS.md) | 维护纪律、门闩、阈值 |

---

## 0. 总览

| # | 配置项 | 找谁 | 写到哪里 | 进 git |
|---|--------|------|----------|--------|
| 1 | 紫鸟组织邀请 | 紫鸟组织管理员 | 用手机号登录紫鸟 | — |
| 2 | 目标店铺授权 | 紫鸟组织管理员 | 紫鸟账号侧 | — |
| 3 | 紫鸟客户端安装包（GUI） | 技术人员 / 管理员 | 本机安装 | — |
| 4 | 紫鸟开放平台 apiKey | 技术人员 / 管理员（**已有已审核应用**） | `ziniao-cli config init` | 否 |
| 5 | 终端识别码绑定 | 本机获取，管理员在开放平台绑定 | open.ziniao.com → 该应用 → 终端管理 | — |
| 6 | Node ≥ 18、Python 3.9+ | 同事自装（官网包） | 本机 | — |
| 7 | `@ziniao-open/cli` | 同事自装 | 本机（npm 全局） | — |
| 8 | 飞书 App Secret ×2 | 管理员 | 根目录 `config.toml` | 否（gitignore） |
| 9 | LLM API Key（语言识别） | 管理员 / 自备 | 根目录 `.env` | 否（gitignore） |
| 10 | 内容审查密钥（可选） | 管理员 | `.env` / `config.toml` | 否（gitignore） |
| 11 | 前端构建产物 | 技术人员执行一次构建 | `assistant/web/static/` | 否（gitignore） |

**明确不需要申请**：WebDriver、`ZINIAO_*` 企业账密、`127.0.0.1:16851`、独立部署 `video-analysis-api`。

---

## 1. 紫鸟侧（重点）

### 1.1 进组织与店铺授权

1. 把**手机号**给紫鸟组织管理员，等对方邀请进组织，再用该手机号登录。
2. **禁止**在被邀请前用该手机号自己注册紫鸟。
3. 让对方授权要操作的店铺（本仓默认会碰下面两家，实际授权哪家取决于要跑哪些入口）：

| 店铺 | storeId | 哪个入口默认用它 |
|------|---------|------------------|
| 跨境1号店（Lingerie Outlet） | `27437742526069` | 1/2/3 号入口：未传 `--store-id` 且无法从 running 唯一解析时的测试默认 |
| 跨境2号店 | `27506607043054` | 0 号入口：工作台没有任何 running 店时的默认开店目标 |

4. **生产 / 多店必须显式传 `--store-id`**；工作台只开着一家时会自动识别。
5. 使用纪律：工作台一次只留一家店；**不要两人同时操作同一家店**（页面互斥，会报 `store-busy`）。

流程细节可参考相邻项目同款文档 `../zn_daren/账号配置.md`。

### 1.2 客户端（GUI）

- macOS：从「应用程序」打开紫鸟；Windows：开始菜单打开。
- 用本人账号登录，工作台能看见店。
- **只走 GUI**（有窗口）。不要在 WEBDRIVER 模式下跑本仓。

### 1.3 开放平台 apiKey

- 要的是[紫鸟开放平台](https://open.ziniao.com)里**某个已审核应用**的 apiKey：给 `ziniao-cli` 鉴权本机 ZClaw。
- 它**不是**紫鸟登录密码、不是 TikTok Shop 店后台账密、也不是 WebDriver 的 `ZINIAO_*`。
- 向技术人员/管理员要企业里**已有**那一把；**不要新建应用**（新建要管理员在开放平台审核，除非对方明确让你建）。
- 写入：

```bash
ziniao-cli config init     # 选「手动输入 Key」
ziniao-cli config show     # 确认已保存；不要把完整 Key 打到终端
```

- 禁止发到聊天、禁止写入仓库。

### 1.4 终端识别码绑定

`config init` 成功只表示本地有配置。`doctor` / `store list` 仍报 API Key 认证失败时，把**这台电脑**的终端识别码绑到签发该 Key 的应用上：

1. **获取**：紫鸟 GUI 已登录 → 顶栏「软件设置」→ 左侧「本地设置」→「终端信息」→「终端识别码」→ 复制。
2. **绑定**：浏览器打开 [open.ziniao.com](https://open.ziniao.com) → 进入**这把 apiKey 对应的那个应用** →「终端管理」→ 绑上刚复制的识别码。自己进不去该应用时，把识别码发给技术人员代绑。
3. 换电脑或重装客户端后识别码会变，**要再绑一次**。

### 1.5 ZClaw 能力检查

本仓只通过 `ziniao-cli zclaw invoke` 调 6 个工具：

| 工具 | 用途 |
|------|------|
| `list_stores` | 列出账号下的店 |
| `extract_data`（`mode=running`） | 查当前 running 的店 |
| `open_store` / `close_store` | 0 号开店（带 debugPort）/ 重开前关闭目标店 |
| `execute_script` | 页面探活与所有页面读写 |
| `visit_page` | 详情/订单等页面跳转 |

如果现有 apiKey 所属应用在开放平台没有开通这些能力，由管理员在开放平台为该应用处理。

### 1.6 通道与端口（不要配错）

| | 本仓库 | 不要走 |
|--|--------|--------|
| 客户端 | **GUI**（有窗口） | WebDriver（通常无窗口） |
| 口 | `127.0.0.1:9481`（ZClaw，随 GUI 启动） | `127.0.0.1:16851` |
| 凭证 | `config init` 的 apiKey | `ZINIAO_*` 企业账密 |

- 必须保持紫鸟 GUI 开着，ZClaw 才可用。
- 禁止 `ziniao-cli page extract --mode running`（可能触发 `runtime.reopen`）。

### 1.7 验收命令

macOS：

```bash
python3 --version
node -v && npm -v
ziniao-cli --version
ziniao-cli doctor
ziniao-cli store list --format table
zsh scripts/ziniao-status.sh        # 须打印 mode: GUI
```

Windows（cmd，不要 Git Bash）：

```bat
py -3 --version
where py
node -v
where node
where ziniao-cli
ziniao-cli --version
ziniao-cli doctor
ziniao-cli store list --format table
```

期望结果：

- `store list` 能看见要操作的那家店。
- `doctor` 连不上 Bridge → 先保证紫鸟 GUI 已打开。
- macOS `ziniao-status.sh` 须打印 `mode: GUI`；Windows 不跑该脚本，目视确认是普通工作台窗口。

### 1.8 已知风险：紫鸟环境被 TikTok 判插件流量

达人详情接口报 `code=100000` / `Please remove the plugin and try again` 时，实测触发点是**紫鸟浏览器环境本身**（自带扩展主世界 hook），不是脚本 bug、也不是平台故障：

- 卸载业务插件、禁用花漾 TK 后仍复现；官方详情页同样报「数据加载失败」。
- 代码侧已有系统级熔断：同一批连续 3 行命中即停止逐行 API 与 DOM 回退，剩余转 `needs_review`，不要反复重跑整批。
- 处置方向：找紫鸟确认/升级指纹，或用干净浏览器 profile 复测。详见 `达人资料补齐接口故障排查-20260906.md` 顶部更正。

另：首次 `open_store` 可能触发浏览器内核下载，可能要几分钟，属正常。

---

## 2. 本机基础环境（同事自己装，不用申请）

| 项 | 要求 | 注意 |
|----|------|------|
| Node.js | ≥ 18（官网 LTS，自带 npm） | 不要 nvm / n / conda |
| Python | 3.9+，建议 3.11+ | Windows 验收只认 `py -3`；不要微软商店版 |
| ziniao-cli | `npm install -g @ziniao-open/cli` | 装完新开终端跑 `ziniao-cli --version` |

- macOS 装完 Python 后到 `/Applications/Python 3.x/` 跑一次 **Install Certificates.command**。
- **不要手改系统或启动器 PATH**；脚本用 `which` 解析绝对路径。
- 全部装完关掉所有终端/黑窗口，新开再验收（Windows 双击 `.bat` 找不到命令时注销或重启一次）。

---

## 3. 仓库侧配置（都不进 git，需单独私发）

### 3.1 `config.toml`（飞书）

```bash
cp config.toml.example config.toml      # Windows: copy config.toml.example config.toml
```

| 段 | 用途 | 要填什么 |
|----|------|----------|
| `[feishu]` | 主推表 wiki（条件 2，**只读**） | `app_id = cli_a96b1c39ac789bb4` + 管理员提供的 App Secret |
| `[feishu.bitable]` | 「达人关系管理(新)」**写入** | `app_id = cli_a975ba96ae781cd1` + 另一个 App Secret；`app_token = CJXSbLIQWahB8esVOiscX7j1nLc`、`table_id = tblWT2SRKJ3CEZ5e`；其余保持示例值 |
| `[content_review]` | 内容审查（可选） | 见 3.2 |

- 禁止默认写旧表 `tbl1Sc97Gkpc96xI`；禁止在飞书表里新增列。
- 去重键：红人ID + 寄样产品；无匹配行不新建。

### 3.2 `.env`

```bash
cp .env.example .env
```

| 变量 | 用途 | 必填 |
|------|------|------|
| `LLM_BASE_URL` / `LLM_API_KEY` / `LLM_MODEL_ID` | 达人简介语言识别（英语/西语），默认 DeepSeek `deepseek-v4-flash` | 是（语言识别） |
| `CONTENT_REVIEW_ENABLED` | 内容审查开关（默认开、缺证据 fail-closed） | 否 |
| `TIKHUB_API_KEY` / `ARK_API_KEY` / `ARK_MODEL` | 视频/内容审查采集与视觉模型 | 否 |
| `FFMPEG_PATH` | 抽帧用 FFmpeg 绝对路径（不装也能跑其它功能） | 否 |

内容审查限额（`max_visual_videos` 等）**只允许调小**，超过上限会直接报配置错误。

### 3.3 前端构建（新电脑 / 前端更新后一次）

```bash
npm install
npm run build:web          # 也可只跑 build:console / build:auto
```

产物在 `assistant/web/static/console/`、`assistant/web/static/auto-approval/`，已被 gitignore，新环境必须重建；业务员日常不需要 Node。

### 3.4 交接时不要外传

`config.toml`、`.env`、`exports/`（含达人名单与审核证据）、`logs/`、构建产物。密钥一律不进聊天、不进 git。

---

## 4. 一次性办理顺序（打勾用）

- [ ] 1. 手机号给紫鸟管理员，等邀请进组织后登录
- [ ] 2. 授权目标店铺（至少一家；多店需显式 `--store-id`）
- [ ] 3. 拿客户端安装包，装 GUI，登录并看见工作台
- [ ] 4. 装 Node ≥ 18、Python 3.9+，`npm install -g @ziniao-open/cli`
- [ ] 5. 拿开放平台 apiKey，`ziniao-cli config init` 手动输入
- [ ] 6. 取本机终端识别码，请管理员在开放平台绑定到该应用
- [ ] 7. 验收：`ziniao-cli doctor`、`store list` 看见店、`mode: GUI`
- [ ] 8. 拿飞书两个 App Secret 填 `config.toml`；填 `.env`（LLM Key）
- [ ] 9. `npm install && npm run build:web`
- [ ] 10. 双击 0 号开店 → 登录 TikTok Shop → 双击 1 号只出名单，试跑成功

---

## 5. 使用前必读的运行纪律

| 事项 | 规则 |
|------|------|
| 开店 | 日常先跑 0 号 `--reopen`（带 debugPort）；工作台按钮直接开的店没有 debugPort，1/2/3 往往要跑两回 |
| 导航 | 1/2/3 不重开、不切店，失败只报错；有其他店 running 时 0 号也拒绝切店 |
| 写操作 | 只有 `--execute --yes`（+ 可选 `--write-feishu`）才会批准 / 发私信；**平台同意与已发私信不可撤销** |
| 时间门 | 物流脚本北京时间 16:00 前拒绝；只在确定测试时用 `--force` |
| 并发 | 不要和网页任务同时跑；网页写任务运行中不提供取消 |
| 登录 | 紫鸟登录 ≠ 已登录商家中心；店铺窗口里的 TikTok Shop 是另一套账密（首次常有验证码） |

---

## 6. 相关文档

| 文档 | 内容 |
|------|------|
| [快速开始.md](./快速开始.md) | 装机步骤、安装包、验收命令 |
| [README.md](./README.md) | 日常双击流程、出问题怎么办 |
| [AGENTS.md](./AGENTS.md) | 门闩、阈值、页面契约（维护用） |
| `样品申请筛查sop/` | 业务规则原文 |
| `../zn_daren/账号配置.md`、`../zn_daren/快速开始.md` | 相邻项目的同款组织邀请 / apiKey / 终端识别码流程 |
| `达人资料补齐接口故障排查-20260906.md` | 紫鸟环境指纹问题（`code=100000`）实测记录 |
